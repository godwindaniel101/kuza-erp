# Marketing website (Next.js). Copy to `.env`.
# The site is public and reads no runtime secrets.
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
# Operator-portal origin for Sign in / Start free (auth lives on the app, not here).
NEXT_PUBLIC_APP_URL=http://localhost:5001
