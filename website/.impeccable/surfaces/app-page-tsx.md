---
version: 1
slug: "app-page-tsx"
primary_target: "app/page.tsx"
related_targets: ["app/features/page.tsx","app/ai/page.tsx","app/industries/page.tsx","app/pricing/page.tsx"]
---

# Surface brief — marketing site (home + features/ai/industries/pricing)

Scope: the public marketing site (Next.js app router, 5 routes). Visitor mode: Persuade.

Audience & job: African multi-branch SMB owners/managers stitching together POS + spreadsheet + accountant + DM inbox; job is to believe Kuza runs the whole operation on one truth and sells for them, then start the 14-day all-access trial (`/register`).

Proof strategy: don't claim — demonstrate. Hero shows a WhatsApp DM becoming a structured order/ledger/books ripple (the site's one authored motion moment). Product truths rendered as hand-built UI mockups with demo data explicitly labeled illustrative. Real merchant photography from `public/img/`. Roadmap items (card/mobile-money, pay-by-link, rules auto-clear, pharmacy) always badged, never sold as live.

Chosen direction: user-pinned clean conversion-first SaaS canon at the Bumpa/Paystack craft bar (superseded seed 54a3e2bb's sign-writer direction). Memorable moment: the hero DM→order→books choreography.

Constraints: PRODUCT.md §12 (no invented customers, illustrative prices labeled, no autonomy claims beyond the human approval queue); CTAs point at `/register` / `/login`; craft-floor ban on eyebrows/kickers observed — headings carry their own weight.

Unresolved: real app domain for CTAs (currently relative paths); final per-currency prices when the pricing model lands; social links & legal pages for the footer.
