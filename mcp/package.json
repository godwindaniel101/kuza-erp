{
  "name": "kuza-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Model Context Protocol server exposing Kuza ERP read-only analytics as tools for Claude.",
  "type": "module",
  "bin": {
    "kuza-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.4.0"
  }
}
