# Postgres container config. Copy to `.env` in this folder.
# These must match backend/.env (DB_USERNAME / DB_PASSWORD / DB_NAME).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=erp_db
