-- Initialize Landlord Database for Multi-Tenant ERP System
-- This script creates the landlord database
-- Note: PostgreSQL init scripts run only when the data directory is empty (first initialization)
-- If the database already exists, you need to create it manually

-- Create landlord database
CREATE DATABASE erp_landlord;
